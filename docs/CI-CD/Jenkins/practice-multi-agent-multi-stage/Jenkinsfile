pipeline {
    agent none

    stages {
        stage('Backend') {
            agent{
                docker {
                    image 'maven:3-sapmachine-22'
					args '-v /root/.m2:/root/.m2'
                }
            }
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
}
